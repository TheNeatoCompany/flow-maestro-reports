# Flow-Mobile Maestro Reports

Automated HTML reports from `scripts/maestro-full.sh` runs (Flow-Mobile).

Live index: https://theneatocompany.github.io/flow-maestro-reports/

Each run lives under a timestamped directory. Retention: last 30 runs (older runs pruned automatically).

